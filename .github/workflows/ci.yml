name: CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  # Job that sets outputs so we can gate visual jobs without using secrets in 'if' (not allowed by GitHub).
  check-secrets:
    name: Check visual test secrets
    runs-on: ubuntu-latest
    outputs:
      chromatic_enabled: ${{ steps.check.outputs.chromatic_enabled }}
      percy_enabled: ${{ steps.check.outputs.percy_enabled }}
      playwright_enabled: ${{ steps.check.outputs.playwright_enabled }}
    steps:
      - id: check
        run: |
          echo "chromatic_enabled=$([ -n \"$CHROMATIC_PROJECT_TOKEN\" ] && echo true || echo false)" >> $GITHUB_OUTPUT
          echo "percy_enabled=$([ -n \"$PERCY_TOKEN\" ] && echo true || echo false)" >> $GITHUB_OUTPUT
          echo "playwright_enabled=$([ -n \"$PERCY_TOKEN\" ] && [ -n \"$VITE_API_KEY\" ] && echo true || echo false)" >> $GITHUB_OUTPUT
        env:
          CHROMATIC_PROJECT_TOKEN: ${{ secrets.CHROMATIC_PROJECT_TOKEN }}
          PERCY_TOKEN: ${{ secrets.PERCY_TOKEN }}
          VITE_API_KEY: ${{ secrets.VITE_API_KEY }}

  test:
    name: Run tests & coverage
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      - name: Run tests with coverage
        run: bunx jest --coverage

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage

  visual-regression-chromatic:
    name: Visual tests (Chromatic)
    runs-on: ubuntu-latest
    needs: [check-secrets]
    if: needs.check-secrets.outputs.chromatic_enabled == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      - name: Publish storybook to Chromatic
        env:
          CHROMATIC_PROJECT_TOKEN: ${{ secrets.CHROMATIC_PROJECT_TOKEN }}
        run: bunx chromatic --project-token $CHROMATIC_PROJECT_TOKEN --exit-once-uploaded

  visual-regression-percy:
    name: Visual tests (Percy)
    runs-on: ubuntu-latest
    needs: [check-secrets]
    if: needs.check-secrets.outputs.percy_enabled == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      # This assumes you have storybook scripts configured. Percy snapshots can be added to your test suite.
      - name: Run Storybook (for Percy)
        run: bun run build-storybook

      - name: Install Percy CLI and upload snapshots
        run: bunx percy snapshot ./storybook-static
        env:
          PERCY_TOKEN: ${{ secrets.PERCY_TOKEN }}

  # Requires GitHub Secrets: PERCY_TOKEN, VITE_BASE_URL, VITE_BALANCE_URL, VITE_API_KEY, VITE_EXPLORER_URL
  visual-regression-playwright:
    name: Visual tests (Playwright + Percy)
    runs-on: ubuntu-latest
    needs: [check-secrets]
    if: needs.check-secrets.outputs.playwright_enabled == 'true'
    env:
      VITE_EXPOSE_STORES_FOR_TESTS: "true"
      VITE_BASE_URL: ${{ secrets.VITE_BASE_URL }}
      VITE_BALANCE_URL: ${{ secrets.VITE_BALANCE_URL }}
      VITE_API_KEY: ${{ secrets.VITE_API_KEY }}
      VITE_EXPLORER_URL: ${{ secrets.VITE_EXPLORER_URL }}
      PERCY_TOKEN: ${{ secrets.PERCY_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      - name: Build
        run: bun run build

      - name: Start preview server
        run: bunx vite preview --port 4173 &

      - name: Install Playwright browsers
        run: bunx playwright install --with-deps

      - name: Run Playwright visual tests
        run: bunx playwright test --config=playwright.config.ts
        env:
          PERCY_TOKEN: ${{ secrets.PERCY_TOKEN }}
